"use client";

import { CourseLibrary } from "@/components/course-library";

/** Member learning desk — same secure catalog as /courses. */
export default function MyCoursesPage() {
  return <CourseLibrary />;
}
